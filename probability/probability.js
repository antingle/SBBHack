const polyregression = require('../polyregression/polyregression.js'); 

// P(x >= 1) = C(x, x_max) * P^x * (1-P)^(x-x_max)
const get_probability = (hours, name, s_max) => {
  
}

module.exports.get_probability = get_probability;